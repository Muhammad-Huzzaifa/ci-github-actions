# ci-github-actions

A simple Python repository for practicing CI with GitHub Actions.

It includes:
- Basic math functions (`add`, `sub`) in `src/math_operations.py`
- Unit tests using `pytest` in `test/operations_test.py`
- A minimal dependency setup in `requirements.txt`

Run tests locally:

```bash
pytest
```
